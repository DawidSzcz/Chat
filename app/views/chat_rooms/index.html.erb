<div style="margin-top: 20px">
<table>
<% @chat_rooms.each do |c| %>

    <tr>
      <td><%=link_to c.name, c%></td>
      <td><%=c.created_at%></td>
    </tr>
<%end%>
</table>
<%if(user_signed_in?)%>
    <%=link_to 'Create new thead', new_chat_room_path %>
<%else%>
    <b>If you want crate new thread, you have to log in.</b>
<%end%>
</div>
<%= render 'chat_rooms/userpanel'%>